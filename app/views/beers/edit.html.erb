<h1>Editing Beer</h1>

<%= render 'form', beer: @beer %>

<%= link_to 'Show', @beer %> |
<%= link_to 'Back', beers_path %>

<%# <h1>Edit To-Do Item</h1>

<%= form_with model: @todo, url: todo_path(@todo), method: :patch, local: true do |f| %>

<%# <% end %> %> %>

<%# human-se.github.io for the edit tutorial %>